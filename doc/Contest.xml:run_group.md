Навигация: [Главная страница](Main_Page.md)/[Система
ejudge](Система_ejudge.md)/[Использование](Использование.md)/[Конфигурационные
файлы](Конфигурационные_файлы.md)/[contest.xml](contest.xml.md)/[run_group](contest.xml:run_group.md)

|                          |                                             |
|--------------------------|---------------------------------------------|
| **Имя элемента**:        | **`run_group`**                             |
| **Содержится в:**        | [`contest`](contest.xml:contest.md) |
| **Может содержать:**     | *нет*                                       |
| **Атрибуты:**            | *нет*                                       |
| **Тип содержимого:**     | группа пользователя в `Unix`                |
| **Может отсутствовать:** | *да*                                        |
| **Может повторяться:**   | *нет*                                       |
| **Примечание:**          | В текущей версии `ejudge` игнорируется      |

**Описание.** Данный элемент позволяет указать группу пользователя в
системе `Unix`, под которым будет запущена программа
[`run`](run.md). Значение элемента предназначено для
использования в программа [`super-serve`](super-serve.md), но в
текущей версии `ejudge` программа `super-serve` не позволяет работать
из-под пользователя `root` и не позволяет запускать другие программы
из-под произвольного пользователя, поэтому значение данного элемента
игнорируется.
