Навигация: [Главная страница](Main_Page.md)/[Система
ejudge](Система_ejudge.md)/[Использование](Использование.md)/[Конфигурационные
файлы](Конфигурационные_файлы.md)/[contest.xml](contest.xml.md)/[users_par_style](contest.xml:users_par_style.md)

|                            |                                             |
|----------------------------|---------------------------------------------|
| **Имя элемента**:          | **`users_par_style`**                       |
| **Содержится в:**          | [`contest`](contest.xml:contest.md) |
| **Может содержать:**       | *нет*                                       |
| **Атрибуты:**              | *нет*                                       |
| **Тип содержимого:**       | строка                                      |
| **Может отсутствовать:**   | *да*                                        |
| **Значение по умолчанию:** | "" (пустая строка)                          |
| **Может повторяться:**     | *нет*                                       |

**Редактирование элемента:** страница *"Editing contest"*, вкладка
*"General settings (contest.xml)"*, блок *"extra HTML attributes for
CGI-programs"*, поле *"HTML attributes for \`users' paragraphs"*.

**Описание.** Данный элемент позволяет задать параметры тега `<p>` в
html-страницах, генерируемых программой `users`. Значение элемента
`users_par_style` добавляется в открывающий тег `<p>`. Закрывающий тег
`</p>` остаётся без изменений.

**Пример.**

<users_par_style>` class="common"`</users_par_style>

Такое значение конфигурационной переменной `users_par_style` приведёт к
тому, что в генерируемых CGI-программой users html-страницах все абзацы
будут помечены следую- щим образом:

`<p class="common">A paragraph</p>`

Обратите внимание на пробел в начале строки значения конфигурационной
переменной `users_par_style`. Если бы он отсутствовал, тег `p` и атрибут
`class` слились в одно слово `pclass`.
