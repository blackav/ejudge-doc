Навигация: [Главная страница](Main_Page "wikilink")/[Система
ejudge](Система_ejudge "wikilink")/[Использование](Использование "wikilink")/[Конфигурационные
файлы](Конфигурационные_файлы "wikilink")/[serve.cfg](serve.cfg "wikilink")/[Конфигурационные
параметры
задач](serve.cfg:problem "wikilink")/[`enable_checker_token`](serve.cfg:problem:enable_checker_token "wikilink")

Данная конфигурационная переменная работает только в режиме оценивающих
проверяющих программ
[`scoring_checker`](Serve.cfg:problem:scoring_checker "wikilink"). Если
конфигурационная переменная `enable_checker_token` установлена в 1, то
проверяющая программа помимо выставления балла за тест должна вывести
произвольную строку-токен. Строка-токен не должна содержать пробельные
символы или нулевой байт. Строка-токен сохраняется в протоколе проверки
и передается в [оценивающую
программу](Оценивающие_программы "wikilink"), если она настроена для
данной задачи.

Так например, проверяющая программа может вывести на стандартный поток
вывода

`3`  
`0K3RgtC+INGC0L7QutC10L0=`

`3` — это оценка текущего теста проверяющей программой, а вторая строка
— это токен, который будет сохранен в протоколе проверки.

Поддерживается начиная с версии
[3.7.7](изменения_в_версии_3.7.7 "wikilink")
