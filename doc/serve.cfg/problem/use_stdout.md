Навигация: [Главная страница](../../main_Page.md)/[Система
ejudge](../../система_ejudge.md)/[Использование](../../использование.md)/[Конфигурационные
файлы](../../конфигурационные_файлы.md)/[serve.cfg](../../serve.cfg.md)/[Конфигурационные
параметры
задач](../problem.md)/[use_stdout](use_stdout.md)

|                            |                                           |
|----------------------------|-------------------------------------------|
| **Имя переменной**:        | **`use_stdout`**                          |
| **Содержится в:**          | [`problem`](../problem.md) |
| **Используется:**          | `run`                                     |
| **Тип содержимого:**       | *boolean*                                 |
| **Может отсутствовать:**   | *да*                                      |
| **Наследуется:**           | *да*                                      |
| **Значение по умолчанию:** | *false*                                   |
| **Может повторяться:**     | *нет*                                     |

**Редактирование элемента:** страница *"Editing contest"*, вкладка
*"Problems (serve.cfg)"*, блок *"Abstract problems"*, поле *"Use
standard output"*, либо страница *"Editing contest"*, вкладка *"Problems
(serve.cfg)"*, блок *"Concrete problems"*, поле *"Use standard output"*.

**Описание.** Если данная конфигурационная переменная установлена в
*true*, решение данной задачи должно выводить результат вычислений на
стандартный поток вывода. Если же конфигурационная переменная
установлена в *false*, решение должно выводить результат вычислений в
файл, имя которого задаётся с помощью конфигурационной переменной
[`output_file`](output_file.md).

Если значение данной переменной в описании неабстрактной задачи не
определено, то наследуется значение переменной из описания
соответствующей абстрактной задачи (если таковая есть). Если и после
этого значение переменной не определено, используется значение по
умолчанию *false*.

**Пример.**

`use_stdout`
