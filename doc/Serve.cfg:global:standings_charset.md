Навигация: [Главная страница](Main_Page.md)/[Система
ejudge](Система_ejudge.md)/[Использование](Использование.md)/[Конфигурационные
файлы](Конфигурационные_файлы.md)/[serve.cfg](serve.cfg.md)/[Глобальные
конфигурационные
параметры](serve.cfg:global.md)/[standings_charset](serve.cfg:global:standings_charset.md)

|                            |                                         |     |
|----------------------------|-----------------------------------------|-----|
| **Имя переменной**:        | **`standings_charset`**                 |     |
| **Содержится в:**          | [`global`](serve.cfg:global.md) |     |
| **Используется:**          | `serve`                                 |     |
| **Тип содержимого:**       | *string*                                |     |
| **Может отсутствовать:**   | *да*                                    |     |
| **Значение по умолчанию:** | совпадает со значением `charset`        |     |
| **Может повторяться:**     | *нет*                                   |     |

**Редактирование элемента:** страница *"Editing contest"*, вкладка
*"Global settings (serve.cfg)"*, блок *"Advanced settings"*, поле
*"Stadings character set"*.

**Описание.** Данная конфигурационная переменная задаёт кодировку, в
которой генерируется статическая таблица результатов турнира.
Статическая таблица результатов — это html-файл, генерируемый в каталоге
файлов состояния сервера турнира, который обновляется после каждой
посылки участника. Назначение этого файла — дать возможность всем
желающим просматривать текущие результаты турнира. По умолчанию
предполагается, что кодировка этого файла совпадает с глобальной
кодировкой сервера турнира (см. конфигурационную переменную
[`charset`](serve.cfg:global:charset.md)). Если переменная
`standings_charset` задаёт другую кодировку, выполняется перекодирование
файла.

**Пример.**

`charset = cp1251`
